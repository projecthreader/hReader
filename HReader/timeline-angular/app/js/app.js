'use strict';

var Timeline = angular.module('Timeline', []);

      Timeline.directive('sparkline', function ( /* dependencies */ ) {

});

      
/* App Module 
angular.module('timeline', []).
  config(['$routeProvider', function($routeProvider) {
  $routeProvider.
      when('/', {templateUrl: 'partials/day.html',   controller: TimelineListCtrl
  }).
      when('/week', {templateUrl: 'partials/week.html', controller: TimelineWeekCtrl
  }).
      when('/month', {templateUrl: 'partials/month.html', controller: TimelineWeekCtrl
      otherwise({redirectTo: '/'
  }).
      when('/week', {templateUrl: 'partials/week.html', controller: TimelineWeekCtrl
  });
}]);
*/